import { makeStyles } from '@material-ui/styles'

const useStyles = makeStyles((theme) => ({
  hidden: {
    display: 'none',
  },
  categoryDropZone: {
    width: '212px',
    height: '100vh',
  },
}))

export default useStyles
